version: '3.8'
services:
  app:
    build: .
    ports:
      - "3323:3323"
    environment:
      - PORT=3323
      - DATABASE_URL=file:/app/data/dev.db
      - NEXT_TELEMETRY_DISABLED=1
    volumes:
      - ./data:/app/data
      - ./public/uploads:/app/public/uploads
